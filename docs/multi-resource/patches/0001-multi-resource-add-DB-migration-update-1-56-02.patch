From 54966b526e26e7b84e74ff83438f0143412d28fd Mon Sep 17 00:00:00 2001
From: christianmoormann <christianmoormann@users.noreply.github.com>
Date: Wed, 2 Sep 2026 11:54:53 +0200
Subject: [PATCH 1/7] multi-resource: add DB migration update-1-56-02

New junction table planning_periode_ressource (composite PK, FKs ON DELETE
CASCADE to planning_periode and planning_ressource), back-filled from the
existing planning_periode.ressource_id. New planning_config row
SOPLANNING_OPTION_RESSOURCES_MULTIPLE (default '0'). version.txt -> 1.56.02.
---
 sql/update/update-1-56-02.txt | 19 +++++++++++++++++++
 version.txt                   |  2 +-
 2 files changed, 20 insertions(+), 1 deletion(-)
 create mode 100644 sql/update/update-1-56-02.txt

diff --git a/sql/update/update-1-56-02.txt b/sql/update/update-1-56-02.txt
new file mode 100644
index 0000000..6f264f2
--- /dev/null
+++ b/sql/update/update-1-56-02.txt
@@ -0,0 +1,19 @@
+-- update 1.56.02 : several resources per task (opt-in feature)
+-- adds option SOPLANNING_OPTION_RESSOURCES_MULTIPLE (default 0, behaviour unchanged)
+-- adds junction table planning_periode_ressource, back-filled from planning_periode.ressource_id
+-- planning_periode.ressource_id is kept and always holds the first / primary resource
+
+CREATE TABLE IF NOT EXISTS `planning_periode_ressource` (
+  `periode_id` int(11) NOT NULL,
+  `ressource_id` varchar(20) NOT NULL DEFAULT '' COLLATE 'latin1_general_ci',
+  PRIMARY KEY (`periode_id`, `ressource_id`),
+  INDEX `idx_pr_ressource_id` (`ressource_id`),
+  CONSTRAINT `fk_pr_periode` FOREIGN KEY (`periode_id`) REFERENCES `planning_periode` (`periode_id`) ON DELETE CASCADE ON UPDATE CASCADE,
+  CONSTRAINT `fk_pr_ressource` FOREIGN KEY (`ressource_id`) REFERENCES `planning_ressource` (`ressource_id`) ON DELETE CASCADE ON UPDATE CASCADE
+) ENGINE = InnoDB CHARACTER SET = latin1 COLLATE = latin1_general_ci ROW_FORMAT = Dynamic;
+
+INSERT INTO `planning_periode_ressource` (`periode_id`, `ressource_id`) SELECT p.`periode_id`, p.`ressource_id` FROM `planning_periode` p INNER JOIN `planning_ressource` r ON r.`ressource_id` = p.`ressource_id` WHERE p.`ressource_id` IS NOT NULL AND p.`ressource_id` <> '';
+
+INSERT INTO `planning_config`(`cle`, `valeur`, `commentaire`) VALUES ('SOPLANNING_OPTION_RESSOURCES_MULTIPLE', '0', 'Allow several resources per task, requires SOPLANNING_OPTION_RESSOURCES');
+
+UPDATE  `planning_config` SET  valeur ='1.56.02' WHERE  `planning_config`.`cle` =  'CURRENT_VERSION';
diff --git a/version.txt b/version.txt
index 87dcde6..753cc42 100644
--- a/version.txt
+++ b/version.txt
@@ -1 +1 @@
-1.56.01
\ No newline at end of file
+1.56.02
\ No newline at end of file
-- 
2.55.0.windows.5

